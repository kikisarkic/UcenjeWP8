create database bonavia;
use bonavia;

create table emezeta(
sifra int primary key,
ime varchar (50),
prezime varchar (50),
adresa varchar (50)
);

create table tenje(   
sifra int primary key,
ime varchar (50),
prezime varchar (50),
adresa varchar (50)
);

create table lio(
sifra int primary key,
ime varchar (50),
prezime varchar (50),
adresa varchar (50)
);

create table antunovac(
sifra int primary key,
ime varchar (50),
prezime varchar (50),
adresa varchar (50)
);
